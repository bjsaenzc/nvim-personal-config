-- Winbar breadcrumbs (P3-10). Replaces the unmaintained barbecue.nvim;
-- uses the native winbar and built-in LSP/treesitter sources.
return {
  "Bekaboo/dropbar.nvim",
  event = "VeryLazy",
  opts = {},
}
